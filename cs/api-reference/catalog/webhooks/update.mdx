---
title: 'Aktualizovat webhook'
api: 'PATCH /catalog/webhooks/{id}'
description: 'Aktualizace webhook odběru'
---

Aktualizace URL, událostí nebo stavu aktivace existujícího webhook odběru.

<ParamField path="id" type="string" required>
  ID webhook odběru.
</ParamField>

<ParamField body="url" type="string">
  Aktualizovaná URL endpointu.
</ParamField>

<ParamField body="events" type="array">
  Aktualizovaný seznam událostí. Platné hodnoty: `product.created`, `product.updated`, `product.deleted`.
</ParamField>

<ParamField body="enabled" type="boolean">
  Aktivace nebo deaktivace webhooku bez jeho smazání.
</ParamField>

<ParamField body="description" type="string">
  Aktualizovaný popis.
</ParamField>

<RequestExample>
```typescript TypeScript
const updated = await lasso.catalog.webhooks.update('wh_abc123...', {
  events: ['product.created', 'product.updated', 'product.deleted'],
  enabled: false,
});
```

```python Python
updated = client.catalog.webhooks.update(
    "wh_abc123...",
    events=["product.created", "product.updated", "product.deleted"],
    enabled=False,
)
```

```bash cURL
curl -X PATCH "https://hub.banditshq.com/api/v1/catalog/webhooks/wh_abc123..." \
  -H "Authorization: Bearer lasso_..." \
  -H "Content-Type: application/json" \
  -d '{"enabled": false}'
```
</RequestExample>

<ResponseExample>
```json
{
  "id": "wh_abc123...",
  "url": "https://example.com/webhook",
  "events": ["product.created", "product.updated", "product.deleted"],
  "enabled": false,
  "description": "Sync to ERP",
  "created_at": "2026-05-20T08:00:00Z",
  "updated_at": "2026-05-27T10:30:00Z"
}
```
</ResponseExample>
